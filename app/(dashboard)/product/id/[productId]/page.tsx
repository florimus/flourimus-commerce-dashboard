import Loader from '@/components/ui/loader';
import { Suspense } from 'react';

export default async function ProductDetailsPage() {
  return <Suspense fallback={<Loader />}>strdyfugihjlk</Suspense>;
}
